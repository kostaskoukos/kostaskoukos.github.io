---
interface Props {
  href: string;
}

const { href } = Astro.props;
---

<a
  href={href}
  class="relative after:bg-white after:absolute after:h-[0.1em] after:w-0 after:bottom-0 after:left-0 hover:after:w-full after:transition-all after:duration-300 cursor-pointer"
  ><slot /></a
>
